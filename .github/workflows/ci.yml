name: CI

on:
  pull_request:
    branches:
      - 'main'
      - 'feat/openai'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Dummy success step
        run: echo "CI check passed"
